<h1>Úprava uživatele</h1>
<form method="post" action="{{ currentUrl }}">
    {% include 'components/admin/users/userForm.twig' %}
    {% if editedUserId != loggedUser.id %}
        <div class="mb-3">
            <label for="role">Role</label>
            <select id="role" name="role" class="form-select" required>
                <option value="0" {{ post.role == '0' ? 'selected' }}>Vyučující</option>
                <option value="1" {{ post.role == '1' ? 'selected' }}>Administrátor</option>
            </select>
        </div>
    {% endif %}
    <div class="mb-3">
        <input type="submit" class="btn btn-outline-success" name="admin_users_edit_form" value="Upravit">
    </div>
</form>